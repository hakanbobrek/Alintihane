<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hak4.alnthane">

    <application
        android:allowBackup="true"
        android:icon="@drawable/kitap_r"
        android:label="@string/app_name"
        android:roundIcon="@drawable/kitap"
        android:supportsRtl="true"
        android:theme="@style/Theme.Alıntıhane">

        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDO-LIt8TXeE_2kEGStwVdJMtIH9qGSKtI" />

        <activity
            android:name=".Adres"
            android:exported="false"
            android:label="@string/title_activity_adres" />
        <activity
            android:name=".Hakkimizda"
            android:exported="false" />
        <activity
            android:name=".Kitaparama"
            android:exported="false" />

        <activity
            android:name=".Alintieklemee"
            android:exported="false" />

        <activity
            android:name=".Alintilar"
            android:exported="false" />

        <activity
            android:name=".Kaydol"
            android:exported="false" />
        <activity
            android:name=".Giris"
            android:exported="false" />
        <activity
            android:name=".SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Anasayfa" />
    </application>

</manifest>